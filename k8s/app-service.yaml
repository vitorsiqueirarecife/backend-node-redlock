apiVersion: v1
kind: Service
metadata:
  name: leader-election-challenge
  namespace: leader-election
spec:
  type: LoadBalancer
  selector:
    app: leader-election-challenge
  ports:
    - port: 3000
      targetPort: 3000
